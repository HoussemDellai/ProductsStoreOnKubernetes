apiVersion: v1
kind: Service
metadata:
  name: {{ include "productsstore.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "productsstore.name" . }}
    helm.sh/chart: {{ include "productsstore.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "productsstore.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
